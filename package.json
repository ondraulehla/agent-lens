{
  "name": "agent-lens",
  "private": true,
  "version": "0.1.0",
  "description": "Drop an AI agent session transcript (JSONL) and see what actually happened — timeline of tool calls, tokens, cost and failures. Fully client-side.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/react": "^19.2.4",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.4",
    "vitest": "^4.1.10"
  }
}
